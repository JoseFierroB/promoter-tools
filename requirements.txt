# promoter-tools — Python dependencies
# Reference for non-pixi users; the canonical environment is pixi (pixi.toml).
# Core environment:
numpy>=1.21
pandas>=1.0
scipy>=1.9.0
scikit-learn>=1.0,<1.3
matplotlib>=3.5
biopython>=1.87
openpyxl>=3.0
tqdm>=4.70.0
psutil>=5.9.0
progressbar2>=4.5.0
xgboost>=3.2.0
nvidia-ml-py>=11

# Optional — tool-specific (install only what you need):
# MEME/FIMO:    meme>=5.5.9
# LCNN:         tensorflow==2.6.0 (tools/Promoters env; root env uses tensorflow>=2.15)
# iPro-MP:      torch>=2.9 (CUDA 12 bundles — sm_120/RTX 5090 support), transformers>=4.29, tokenizers
