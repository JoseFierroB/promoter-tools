name = "CNNProm (E. coli)"
short = "cnnprom"
category = "DL"
pixi_env = "tools/Promoters/pixi.toml"
gpu_capable = true
n_sequences = 1988

model_paths = ["output/predictions/cnnprom_ecoli_model.keras"]

outputs = [
    "output/predictions/cnnprom_pos.csv",
    "output/predictions/cnnprom_neg.csv",
]
